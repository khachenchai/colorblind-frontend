type Centroid = {
    centroids: [number, number],
    r_setas: {
        prota: { r: number; seta: number },
        deutera: { r: number; seta: number },
        trita: { r: number; seta: number },
    },
};

type RSeta = {
    r: number;
    seta: number
}